

// const fiyat = [100, 250, 50, 89];

// let sonuc=0;
// fiyat.forEach((a)=>(fiyat*10)/100 + a);
// console.log(sonuc);

    


// const array = [`kalem`, `silgi`, `mouse`, `screen`, `glasses`];

// console.log(array.map((name)=>name.toLocaleLowerCase()));
// const fiyatlar = [100, 250, 50, 89];
// let toplam1 = 0;

// fiyatlar.forEach((deger, indis, dizi) => {
//   toplam1 += deger;
//   console.log(`${indis}.iterasyon toplami:${toplam1}`);
//   dizi[indis] = deger + 10;
// });
// // console.log(fiyatlar);





// const numbers =[1,2,2,3,3,3,4,5,"ali","veli"];
// numbers.forEach(value,index,dize) => {
//  if(typeof value == typeof Number()){ 
//   array[index] = value*3;
// }
// console.log(numbers);



// sadece number olanları 3 ile çarpalım
// stringleri slice la
// Calculate the mean and median values of the number elements from the input array.
// The given input is a string of multiple words with a single space between each of them. Abbreviate the name and return the name initials.
// ! Devs like to abbreviate everything: k8s means Kubernetes, a11y means accessibility, l10n means localization. You get the Dev numeronyms by taking the first and the last letter and counting the number of letters in between. Words that have less than 4 letters aren't abbreviated, because that would just be odd. The input is a sentence, and you should abbreviate every word that is 4 letters long or longer. There won't be any punctuation in the sentence. const input = 'Every developer likes to mix kubernetes and javascript'; result 'E3y d7r l3s to mix k8s and j8t'
// If the given input is a number, you should return the factorial of that number. The factorial of a natural number n is the product of the positive integers less than or equal to n. So, 2! = 2, 3! = 6, 4! = 24 and so on.


// const  fiyatlar =[100,250,50,89];

// const fiyatlarBuyuk90 = fiyatlar.filter((f) => f<90).forEach((f)=>console.log(f););
// console.log(fiyatlarBuyuk90);

// const fiyatlar = [100, 250, 50, 89];

// fiyatlar.filter((x)=> x<110).map((x)=> x*1.1).forEach((x)=>console.log(x.toFixed(1)));

// //* maaslar 4000'den düsük olanlara %50 zam yapmak istiyoruz //* ve bunu ayri dizi olarak saklamak istiyoruz. //
//  const maaslar = [3000,3250,1000, 5000, 4000, 6000, 6500];
// const liste=[];
//  maaslar.filter((x)=>x<4000).map((x)=>x*1.5).forEach((x)=>liste.push(x));
//  console.log(liste);


 